archivesBaseName = "${rootProject.name}-test"

configurations {
}

dependencies {
    compileOnly project( path: ':wrapper', configuration: 'namedElements' )
    compileOnly project( path: ':core', configuration: 'namedElements' )
    runtimeOnly project(':fatjar')
}