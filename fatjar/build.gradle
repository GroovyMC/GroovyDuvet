plugins {
    alias libs.plugins.quilt.loom
}

project.archivesBaseName = "${rootProject.archivesBaseName}"

configurations {
    include
    implementation.extendsFrom include
}

dependencies {
    include project(':core')
}